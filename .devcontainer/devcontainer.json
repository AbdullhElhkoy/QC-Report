{
  "name": "QC Report",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0 fonts-noto-core && pip install --upgrade pip && pip install -r requirements.txt && python manage.py migrate",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Django dev server",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "batisteo.vscode-django"
      ]
    }
  }
}
